#include"main.h"
char **strtow(char *str)
{
	int len;
	int num_words;
	int i;

	if (str == NULL || str == "")
		return(NULL);

	len = _strlen(str);

	for(i=0; i<len; i++)
	{
		if (_strchr()) // to be continued
	}

}