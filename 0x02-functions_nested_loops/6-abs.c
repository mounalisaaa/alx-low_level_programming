#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 *  @n: int type number
 * Return: 1 or 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
